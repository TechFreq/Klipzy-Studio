# Klipzy Studio - Python server dependencies

# Core
pydantic>=2.0.0
numpy>=1.24.0
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
# Hardware detection for the model recommender (RAM/CPU); system_check falls
# back gracefully if absent, but the AI-model auto-pick needs it to read RAM.
psutil>=5.9.0

# Transcription (3 backends, auto-selected in server/core/transcriber.py)
#   1. mlx-whisper   - native MLX acceleration, fastest on Apple Silicon (M-series).
#                      Marker keeps it macOS-arm64 only; a no-op elsewhere.
#   2. faster-whisper - CTranslate2, 3-5x faster than openai-whisper on CPU/CUDA.
#   3. openai-whisper - universal fallback.
mlx-whisper>=0.4.0; sys_platform == "darwin" and platform_machine == "arm64"
faster-whisper>=1.0.0
openai-whisper>=20231117

# Audio energy detection
librosa>=0.10.0
soundfile>=0.12.1

# Computer vision (speaker tracking / smart crop)
opencv-python>=4.8.0
ultralytics>=8.0.0
# torch / torchvision: install separately for your platform:
#   Windows/Linux: pip install torch torchvision (add --index-url https://download.pytorch.org/whl/cu121 for CUDA)
#   macOS: pip install torch torchvision (Apple Silicon gets MPS acceleration automatically)

# Optional local LLM (AI edit chat + LLM highlight discovery)
ollama>=0.1.6

# Development / testing
pytest>=7.0.0
httpx>=0.24.0
